# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# Database Configuration (MongoDB Atlas or local fallback)
MONGODB_URI=mongodb://127.0.0.1:27017/insurance_platform

# JWT Authentication
JWT_SECRET=super_secret_jwt_key_insurance_agent_platform_2026
JWT_EXPIRES_IN=7d

# Free Gmail SMTP (Sends real emails to ANY customer with 0 domain required)
SMTP_USER=
SMTP_PASS=

# Cloudinary (Optional - local filesystem fallback active if unset)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Stripe Payment Link / Checkout (Optional - mock simulation active if unset)
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder

# Resend Email (Alternative provider)
RESEND_API_KEY=
EMAIL_FROM=onboarding@resend.dev
